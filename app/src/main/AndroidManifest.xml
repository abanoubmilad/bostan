<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="abanoubm.bostan">

    <uses-permission android:name="android.permission.read_external_storage" />
    <uses-permission android:name="android.permission.write_external_storage"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <activity
            android:name=".Splash"
            android:theme="@style/Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main" />
        <activity android:name=".DisplaySection" />
        <activity android:name=".SectionChooser" />
        <activity android:name=".CharacterChooser" />
        <activity android:name=".CharSectionChooser" />
        <activity android:name=".DisplaySearchResults" />
        <activity android:name=".Search" />
    </application>

</manifest>
